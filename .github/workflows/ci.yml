name: CI
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Repository check
        run: |
          echo 'No standard package manifest detected.'
          echo 'Detected languages: check repo contents and update this workflow.'
          ls -la

